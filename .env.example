# Sanity CMS Configuration
# Get these values from: https://sanity.io/manage

# Project ID (find in Sanity dashboard)
NEXT_PUBLIC_SANITY_PROJECT_ID=your_project_id

# Dataset (usually 'production' or 'development')
NEXT_PUBLIC_SANITY_DATASET=production

# API Token (create in Sanity → API → Tokens → Add token)
# Permissions: Viewer (read-only access)
SANITY_API_TOKEN=your_read_token

# Webhook Secret (generate with: openssl rand -hex 32)
# Used to validate webhook requests from Sanity
# CRITICAL: Use a strong random string (min 32 characters)
SANITY_WEBHOOK_SECRET=generate_random_string_min_32_chars

# Google Tag Manager (optional - for tracking)
NEXT_PUBLIC_GTM_ID=GTM-XXXXXXX

# Vercel Configuration (automatic on Vercel deploy)
# NEXT_PUBLIC_VERCEL_URL=your-domain.vercel.app
